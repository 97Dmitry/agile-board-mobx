import { Outlet } from "react-router-dom";

const AuthorizationLayout = () => {
  return (
    <>
      <div>AuthorizationLayout</div>
      <Outlet />
    </>
  );
};

export default AuthorizationLayout;
